{
  "name": "iconv_lite",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/iconv-lite": "^0.0.1",
    "hono": "^4.4.7",
    "iconv-lite": "^0.6.3"
  },
  "scripts": {
    "start": "bun run src/main.ts",
    "package":"rm -rf dist && bun build --entrypoint src/main.ts --outdir ./dist --target bun"
  }
}